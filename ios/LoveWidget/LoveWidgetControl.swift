// Intentionally left empty.
//
// The Xcode template generated a "Control Widget" here (iOS 18+ Control Center
// widget). We don't need it for this demo, and it targets a newer OS than our
// iOS 16 minimum, so we remove its code to keep the build clean.
