// Intentionally left empty.
//
// The widget and its @main WidgetBundle are both declared in LoveWidget.swift.
// The Xcode template originally generated a second `@main struct LoveWidgetBundle`
// here, which conflicted ("Invalid redeclaration"). We keep a single source of
// truth in LoveWidget.swift, so this file has no code.
