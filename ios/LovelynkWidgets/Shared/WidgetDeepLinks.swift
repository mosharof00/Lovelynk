import Foundation

/// Reserved deep-link URLs for interactive widgets.
///
/// Heartbeat / Kiss / Emoji no longer attach `.widgetURL` so a tap simply
/// opens the app (send flows live on the Flutter details screens).
enum WidgetDeepLinks {}
